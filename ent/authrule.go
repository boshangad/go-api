// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/boshangad/v1/ent/authrule"
)

// AuthRule is the model entity for the AuthRule schema.
type AuthRule struct {
	config
	// ID of the ent.
	ID int `json:"id,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AuthRule) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case authrule.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type AuthRule", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AuthRule fields.
func (ar *AuthRule) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case authrule.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ar.ID = int(value.Int64)
		}
	}
	return nil
}

// Update returns a builder for updating this AuthRule.
// Note that you need to call AuthRule.Unwrap() before calling this method if this AuthRule
// was returned from a transaction, and the transaction was committed or rolled back.
func (ar *AuthRule) Update() *AuthRuleUpdateOne {
	return (&AuthRuleClient{config: ar.config}).UpdateOne(ar)
}

// Unwrap unwraps the AuthRule entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ar *AuthRule) Unwrap() *AuthRule {
	tx, ok := ar.config.driver.(*txDriver)
	if !ok {
		panic("ent: AuthRule is not a transactional entity")
	}
	ar.config.driver = tx.drv
	return ar
}

// String implements the fmt.Stringer.
func (ar *AuthRule) String() string {
	var builder strings.Builder
	builder.WriteString("AuthRule(")
	builder.WriteString(fmt.Sprintf("id=%v", ar.ID))
	builder.WriteByte(')')
	return builder.String()
}

// AuthRules is a parsable slice of AuthRule.
type AuthRules []*AuthRule

func (ar AuthRules) config(cfg config) {
	for _i := range ar {
		ar[_i].config = cfg
	}
}
