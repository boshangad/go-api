// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/boshangad/v1/ent/authitemchild"
)

// AuthItemChildCreate is the builder for creating a AuthItemChild entity.
type AuthItemChildCreate struct {
	config
	mutation *AuthItemChildMutation
	hooks    []Hook
}

// Mutation returns the AuthItemChildMutation object of the builder.
func (aicc *AuthItemChildCreate) Mutation() *AuthItemChildMutation {
	return aicc.mutation
}

// Save creates the AuthItemChild in the database.
func (aicc *AuthItemChildCreate) Save(ctx context.Context) (*AuthItemChild, error) {
	var (
		err  error
		node *AuthItemChild
	)
	if len(aicc.hooks) == 0 {
		if err = aicc.check(); err != nil {
			return nil, err
		}
		node, err = aicc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*AuthItemChildMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = aicc.check(); err != nil {
				return nil, err
			}
			aicc.mutation = mutation
			if node, err = aicc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(aicc.hooks) - 1; i >= 0; i-- {
			if aicc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = aicc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, aicc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (aicc *AuthItemChildCreate) SaveX(ctx context.Context) *AuthItemChild {
	v, err := aicc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (aicc *AuthItemChildCreate) Exec(ctx context.Context) error {
	_, err := aicc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (aicc *AuthItemChildCreate) ExecX(ctx context.Context) {
	if err := aicc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (aicc *AuthItemChildCreate) check() error {
	return nil
}

func (aicc *AuthItemChildCreate) sqlSave(ctx context.Context) (*AuthItemChild, error) {
	_node, _spec := aicc.createSpec()
	if err := sqlgraph.CreateNode(ctx, aicc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (aicc *AuthItemChildCreate) createSpec() (*AuthItemChild, *sqlgraph.CreateSpec) {
	var (
		_node = &AuthItemChild{config: aicc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: authitemchild.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: authitemchild.FieldID,
			},
		}
	)
	return _node, _spec
}

// AuthItemChildCreateBulk is the builder for creating many AuthItemChild entities in bulk.
type AuthItemChildCreateBulk struct {
	config
	builders []*AuthItemChildCreate
}

// Save creates the AuthItemChild entities in the database.
func (aiccb *AuthItemChildCreateBulk) Save(ctx context.Context) ([]*AuthItemChild, error) {
	specs := make([]*sqlgraph.CreateSpec, len(aiccb.builders))
	nodes := make([]*AuthItemChild, len(aiccb.builders))
	mutators := make([]Mutator, len(aiccb.builders))
	for i := range aiccb.builders {
		func(i int, root context.Context) {
			builder := aiccb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*AuthItemChildMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, aiccb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, aiccb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, aiccb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (aiccb *AuthItemChildCreateBulk) SaveX(ctx context.Context) []*AuthItemChild {
	v, err := aiccb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (aiccb *AuthItemChildCreateBulk) Exec(ctx context.Context) error {
	_, err := aiccb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (aiccb *AuthItemChildCreateBulk) ExecX(ctx context.Context) {
	if err := aiccb.Exec(ctx); err != nil {
		panic(err)
	}
}
